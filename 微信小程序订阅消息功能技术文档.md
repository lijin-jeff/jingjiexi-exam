# 微信小程序订阅消息功能技术文档

## 1. 文档概述

### 1.1 文档目的
本文档详细介绍微信小程序订阅消息功能的技术实现，包括后端逻辑、API接口和前端实现，旨在帮助开发人员理解、使用和维护该功能。

### 1.2 功能范围
- 支持三种订阅类型：倒计时、版本更新、考试
- 提供统一的订阅/取消订阅接口
- 实现一次性订阅消息发送机制
- 支持多页面订阅模板使用

### 1.3 目标读者
- 后端开发人员
- 前端开发人员
- 测试人员
- 产品经理

## 2. 技术架构

### 2.1 整体架构
```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  微信小程序前端  │     │  后端服务      │     │  微信服务器    │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│  countdown/     │     │  SubscribeLogic│     │  订阅消息接口  │
│  detail.vue     │────▶│  ::subscribe   │────▶│  send?access_token=xxx │
│                 │     │                 │     │                 │
│  common/        │     │  SubscribeLogic│     │  access_token  │
│  update.vue     │────▶│  ::getStatus   │     │  获取接口      │
│                 │     │                 │     │                 │
│  examination/   │     │  SubscribeLogic│     │                 │
│  examinationContent.vue │────▶│  ::sendOneTimeSubscribeMsg │     │                 │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### 2.2 技术栈
- **后端框架**：ThinkPHP 8.0.2
- **前端框架**：Vue.js
- **数据库**：MySQL
- **微信API**：订阅消息接口

## 3. 后端实现

### 3.1 核心逻辑类

#### SubscribeLogic.php
**文件路径**：`c:\Users\44343\jingjiexi\server\app\api\logic\SubscribeLogic.php`

**功能概述**：实现统一的订阅逻辑，处理三种订阅类型的订阅、状态查询和消息发送。

**核心方法**：

| 方法名 | 功能描述 | 参数 | 返回值 |
|--------|----------|------|--------|
| `subscribe()` | 统一订阅/取消订阅 | `array $params` | `array` |
| `getStatus()` | 获取订阅状态 | `array $params` | `array` |
| `getWxAccessToken()` | 获取微信access_token | 无 | `string` |
| `sendOneTimeSubscribeMsg()` | 发送一次性订阅消息 | `array $params` | `Json` |

### 3.2 API接口定义

#### 3.2.1 订阅/取消订阅接口

**请求URL**：`/api/subscribe/recordSubscribe`

**请求方法**：POST

**请求参数**：
| 参数名 | 类型 | 必填 | 描述 |
|--------|------|------|------|
| `type` | string | 是 | 订阅类型：countdown/version_update/exam |
| `related_id` | string | 是 | 关联ID（倒计时ID/版本ID/考试ID） |
| `subscribe_status` | int | 是 | 订阅状态：1-订阅，0-取消订阅 |
| `subscribe_results` | string | 否 | 订阅结果（微信返回的订阅凭证） |
| `template_data` | array | 否 | 模板数据 |
| `openid` | string | 否 | 用户openid |

**返回示例**：
```json
{
  "code": 0,
  "msg": "订阅成功",
  "data": {
    "is_followed": 1,
    "action": "订阅",
    "follow_count": 10
  }
}
```

#### 3.2.2 查询订阅状态接口

**请求URL**：`/api/subscribe/checkCanPush`

**请求方法**：GET

**请求参数**：
| 参数名 | 类型 | 必填 | 描述 |
|--------|------|------|------|
| `type` | string | 是 | 订阅类型 |
| `related_id` | string | 是 | 关联ID |

**返回示例**：
```json
{
  "code": 0,
  "msg": "获取订阅状态成功",
  "data": {
    "is_followed": 1
  }
}
```

#### 3.2.3 发送订阅消息接口

**请求URL**：`/api/subscribe/sendOneTimeSubscribeMsg`

**请求方法**：POST

**请求参数**：
| 参数名 | 类型 | 必填 | 描述 |
|--------|------|------|------|
| `openid` | string | 是 | 用户openid |
| `template_id` | string | 是 | 模板ID |
| `page` | string | 否 | 跳转页面 |
| `message_data` | array | 是 | 消息内容 |
| `type` | string | 是 | 订阅类型 |
| `related_id` | string | 是 | 关联ID |

**返回示例**：
```json
{
  "code": 0,
  "msg": "一次性订阅消息发送成功"
}
```

### 3.3 数据库结构

#### la_user_subscribe表

| 字段名 | 类型 | 描述 |
|--------|------|------|
| `id` | int(11) | 主键ID |
| `user_id` | int(11) | 用户ID |
| `type` | varchar(20) | 订阅类型 |
| `related_id` | varchar(32) | 关联ID |
| `subscribe_status` | tinyint(1) | 订阅状态 |
| `template_id` | varchar(50) | 模板ID |
| `subscribe_time` | bigint(20) | 订阅时间（毫秒） |
| `is_pushed` | tinyint(1) | 是否已推送 |
| `openid` | varchar(50) | 用户openid |
| `template_data` | text | 模板数据 |

### 3.4 订阅类型映射

```php
private const SUBSCRIBE_TYPES = [
    'countdown' => '倒计时',
    'version_update' => '版本更新',
    'exam' => '考试'
];
```

## 4. 前端实现

### 4.1 页面实现

#### 4.1.1 countdown/detail.vue
- **功能**：倒计时页面订阅功能
- **实现要点**：
  - 订阅按钮点击事件处理
  - 订阅模板展示
  - 订阅结果处理
- **核心代码**：
  ```vue
  <button @click="subscribe">订阅倒计时提醒</button>
  
  <script>
  export default {
    methods: {
      async subscribe() {
        const res = await this.$api.subscribe.recordSubscribe({
          type: 'countdown',
          related_id: this.countdownId,
          subscribe_status: 1,
          openid: this.openid
        });
        if (res.code === 0) {
          this.$toast('订阅成功');
        }
      }
    }
  }
  </script>
  ```

#### 4.1.2 common/update.vue
- **功能**：版本更新页面订阅功能
- **实现要点**：
  - 更新通知订阅
  - 订阅状态检查
  - 版本信息展示

#### 4.1.3 examination/examinationContent.vue
- **功能**：考试内容页面订阅功能
- **实现要点**：
  - 考试提醒订阅
  - 考试时间倒计时
  - 订阅状态管理

### 4.2 订阅流程

1. **用户点击订阅按钮**：触发订阅请求
2. **调用微信订阅接口**：弹出订阅模板授权框
3. **用户授权**：微信返回订阅凭证
4. **调用后端API**：记录订阅信息
5. **后端处理**：存储订阅记录到数据库
6. **返回结果**：前端更新订阅状态

### 4.3 订阅模板设计

| 订阅类型 | 模板ID | 模板内容 |
|----------|--------|----------|
| 倒计时 | 模板ID1 | 倒计时提醒：{{keyword1.DATA}}\n事件名称：{{keyword2.DATA}}\n剩余时间：{{keyword3.DATA}}\n备注：{{keyword4.DATA}} |
| 版本更新 | 模板ID2 | 版本更新通知：{{keyword1.DATA}}\n更新内容：{{keyword2.DATA}}\n更新时间：{{keyword3.DATA}}\n备注：{{keyword4.DATA}} |
| 考试 | 模板ID3 | 考试提醒：{{keyword1.DATA}}\n考试名称：{{keyword2.DATA}}\n考试时间：{{keyword3.DATA}}\n备注：{{keyword4.DATA}} |

## 5. 消息发送机制

### 5.1 access_token管理
- 使用微信API获取access_token
- 缓存2小时，避免频繁调用
- 异常处理机制

### 5.2 一次性订阅消息发送流程

1. **获取参数**：openid、templateId、page、messageData等
2. **前置校验**：检查是否可推送，包括订阅记录是否存在、是否已过期
3. **准备消息内容**：构造消息数据
4. **获取access_token**：从缓存或微信API获取
5. **调用微信发送接口**：发送订阅消息
6. **处理发送结果**：根据结果返回响应
7. **更新推送状态**：标记消息已推送

### 5.4 订阅消息发送时间策略

#### 5.4.1 倒计时订阅消息发送时间

**发送时间点**：
- 距离倒计时结束前 **1天**
- 距离倒计时结束前 **3小时**
- 距离倒计时结束前 **30分钟**
- 距离倒计时结束前 **5分钟**

**实现逻辑**：
```php
// 倒计时发送时间计算示例
public function calculateCountdownSendTimes($endTime) {
    $now = time();
    $sendTimes = [];
    
    // 1天前（24小时）
    $oneDayBefore = $endTime - 24 * 3600;
    if ($oneDayBefore > $now) {
        $sendTimes[] = $oneDayBefore;
    }
    
    // 3小时前
    $threeHoursBefore = $endTime - 3 * 3600;
    if ($threeHoursBefore > $now) {
        $sendTimes[] = $threeHoursBefore;
    }
    
    // 30分钟前
    $thirtyMinutesBefore = $endTime - 30 * 60;
    if ($thirtyMinutesBefore > $now) {
        $sendTimes[] = $thirtyMinutesBefore;
    }
    
    // 5分钟前
    $fiveMinutesBefore = $endTime - 5 * 60;
    if ($fiveMinutesBefore > $now) {
        $sendTimes[] = $fiveMinutesBefore;
    }
    
    return $sendTimes;
}
```

#### 5.4.2 版本更新订阅消息发送时间

**发送时间点**：
- 版本更新完成后 **立即发送**（默认）
- 可配置为 **特定时间推送**（如凌晨2点，减少服务器压力）
- 支持 **分批推送**（按用户群体分批发送，避免峰值压力）

**实现逻辑**：
```php
// 版本更新发送时间处理示例
public function handleVersionUpdateSend($versionInfo) {
    $sendConfig = $versionInfo['send_config'] ?? [];
    
    if (isset($sendConfig['send_time'])) {
        // 特定时间发送
        $sendTime = strtotime($sendConfig['send_time']);
        $this->addSendTask('version_update', $versionInfo['id'], $sendTime);
    } elseif (isset($sendConfig['batch_push']) && $sendConfig['batch_push']) {
        // 分批推送
        $this->addBatchSendTask('version_update', $versionInfo['id']);
    } else {
        // 立即发送
        $this->sendVersionUpdateMsg($versionInfo);
    }
}
```

#### 5.4.3 考试订阅消息发送时间

**发送时间点**：
- 考试开始前 **1天**
- 考试开始前 **3小时**
- 考试开始前 **30分钟**
- 考试开始前 **5分钟**
- 考试结束前 **15分钟**（提醒交卷）

**实现逻辑**：
```php
// 考试发送时间计算示例
public function calculateExamSendTimes($examStartTime, $examEndTime) {
    $now = time();
    $sendTimes = [];
    
    // 考试开始前1天
    $oneDayBefore = $examStartTime - 24 * 3600;
    if ($oneDayBefore > $now) {
        $sendTimes[] = ['time' => $oneDayBefore, 'type' => 'exam_start_remind'];
    }
    
    // 考试开始前3小时
    $threeHoursBefore = $examStartTime - 3 * 3600;
    if ($threeHoursBefore > $now) {
        $sendTimes[] = ['time' => $threeHoursBefore, 'type' => 'exam_start_remind'];
    }
    
    // 考试开始前30分钟
    $thirtyMinutesBefore = $examStartTime - 30 * 60;
    if ($thirtyMinutesBefore > $now) {
        $sendTimes[] = ['time' => $thirtyMinutesBefore, 'type' => 'exam_start_remind'];
    }
    
    // 考试开始前5分钟
    $fiveMinutesBefore = $examStartTime - 5 * 60;
    if ($fiveMinutesBefore > $now) {
        $sendTimes[] = ['time' => $fiveMinutesBefore, 'type' => 'exam_start_remind'];
    }
    
    // 考试结束前15分钟
    $fifteenMinutesBeforeEnd = $examEndTime - 15 * 60;
    if ($fifteenMinutesBeforeEnd > $now) {
        $sendTimes[] = ['time' => $fifteenMinutesBeforeEnd, 'type' => 'exam_end_remind'];
    }
    
    return $sendTimes;
}
```

#### 5.4.4 发送时间配置管理

| 配置项 | 描述 | 默认值 |
|--------|------|--------|
| countdown_send_times | 倒计时发送时间点配置 | [86400, 10800, 1800, 300]（秒） |
| exam_start_send_times | 考试开始前发送时间点 | [86400, 10800, 1800, 300]（秒） |
| exam_end_remind_time | 考试结束前提醒时间 | 900（秒，15分钟） |
| version_update_send_immediate | 版本更新是否立即发送 | true |
| batch_push_size | 分批推送每批数量 | 1000 |

#### 5.4.5 发送任务调度

1. **定时任务触发**：使用Cron或其他定时任务工具，每分钟检查一次待发送任务
2. **任务优先级**：
   - 立即发送任务（最高优先级）
   - 距离发送时间不足1分钟的任务
   - 按计划时间顺序排列的任务
3. **任务去重**：确保同一订阅记录不会被重复发送
4. **失败重试**：发送失败后自动重试，重试间隔递增（1分钟、5分钟、15分钟）

**定时任务示例**：
```bash
# 每分钟执行一次发送任务检查
* * * * * php think subscribe:sendTask
```

**发送任务执行逻辑**：
```php
// 发送任务执行示例
public function executeSendTask() {
    // 获取当前时间
    $now = time();
    
    // 查询待发送任务（30秒内需要发送的）
    $tasks = Db::name('subscribe_send_task')
        ->where('send_time', '<=', $now + 30)
        ->where('status', 0)
        ->order('priority desc, send_time asc')
        ->limit(100)
        ->select();
    
    foreach ($tasks as $task) {
        try {
            // 执行发送
            $this->sendOneTimeSubscribeMsg($task);
            
            // 更新任务状态
            Db::name('subscribe_send_task')
                ->where('id', $task['id'])
                ->update(['status' => 1, 'send_time' => $now]);
        } catch (Exception $e) {
            // 处理发送失败
            $this->handleSendFailure($task, $e);
        }
    }
}

### 5.5 订阅消息生命周期管理

#### 5.5.1 授权有效期规则
根据微信官方文档，一次性订阅消息的授权有效期遵循以下规则：

1. **发送后**：一旦发送订阅消息，该次授权立即失效
2. **未发送超时**：如果7天内未发送消息，授权自动失效
3. **重新授权**：授权失效后，需要用户重新点击订阅按钮进行授权

#### 5.5.2 过期处理机制

**后端处理**：
```php
// 在发送前检查授权是否过期
private static function checkSubscriptionExpired($subscribe) {
    // 计算7天前的时间戳（毫秒）
    $sevenDaysAgo = (time() - 7 * 24 * 3600) * 1000;
    // 检查订阅时间是否超过7天
    if ($subscribe['subscribe_time'] < $sevenDaysAgo) {
        // 授权已过期，更新状态
        $subscribe->save(['is_pushed' => -1, 'status' => 'expired']);
        return true;
    }
    return false;
}
```

**前端处理**：
- 定期检查订阅状态
- 授权过期后提示用户重新订阅
- 在关键操作前验证订阅有效性

#### 5.5.3 重新订阅流程

1. **检测授权失效**：前端或后端检测到订阅授权已失效
2. **提示用户重新订阅**：前端显示重新订阅按钮
3. **用户重新授权**：用户点击订阅按钮，再次授权
4. **更新订阅记录**：后端更新订阅记录，重置订阅时间和推送状态
5. **恢复消息发送**：重新获得发送权限

### 5.6 订阅状态管理

| 状态 | 描述 | 处理方式 |
|------|------|----------|
| 0 | 未推送 | 可以发送消息 |
| 1 | 已推送 | 授权已失效，需重新订阅 |
| -1 | 已过期 | 授权已失效，需重新订阅 |
| -2 | 发送失败 | 检查失败原因，可重新发送 |

### 5.7 消息内容示例

```json
{
  "keyword1": {"value": "倒计时提醒"},
  "keyword2": {"value": "新年倒计时"},
  "keyword3": {"value": "24小时"},
  "keyword4": {"value": "请及时查看"}
}
```

## 6. 错误处理

### 6.1 后端错误处理
- 微信API调用异常处理
- 数据库操作异常处理
- 参数验证错误

### 6.2 前端错误处理
- 微信订阅接口调用失败处理
- 后端API调用失败处理
- 用户取消订阅处理

### 6.3 常见错误码

| 错误码 | 描述 | 处理建议 |
|--------|------|----------|
| 43101 | 用户未订阅该模板 | 引导用户重新订阅 |
| 40013 | 无效的appid | 检查appid配置 |
| 40001 | 无效的access_token | 重新获取access_token |
| 1 | 当前不可推送 | 检查订阅状态 |
| 500 | 发送异常 | 查看日志，排查问题 |

## 7. 部署和维护

### 7.1 部署要求
- PHP >= 8.0
- MySQL >= 5.7
- 微信小程序开发者账号
- 已配置订阅模板

### 7.2 配置项

| 配置项 | 描述 | 默认值 |
|--------|------|--------|
| appid | 小程序appid | 无 |
| secret | 小程序secret | 无 |
| template_id | 订阅模板ID | 无 |
| cache_key | access_token缓存键 | wx_access_token |
| cache_expire | 缓存过期时间 | 7200秒 |

### 7.3 日志管理
- 记录access_token获取日志
- 记录消息发送日志
- 记录订阅状态变更日志

## 8. 使用示例

### 8.1 后端API调用示例

```php
// 订阅示例
$subscribeLogic = new SubscribeLogic();
$result = $subscribeLogic->subscribe([
    'type' => 'countdown',
    'related_id' => '123',
    'user_id' => '456',
    'subscribe_status' => 1,
    'openid' => 'openid123'
]);

// 发送消息示例
$subscribeLogic->sendOneTimeSubscribeMsg([
    'openid' => 'openid123',
    'template_id' => 'template_id1',
    'page' => 'pages/index/index',
    'message_data' => [
        'keyword1' => ['value' => '倒计时提醒'],
        'keyword2' => ['value' => '新年倒计时'],
        'keyword3' => ['value' => '24小时'],
        'keyword4' => ['value' => '请及时查看']
    ],
    'type' => 'countdown',
    'related_id' => '123'
]);
```

### 8.2 前端调用示例

```javascript
// 订阅请求
try {
  const res = await wx.requestSubscribeMessage({
    tmplIds: ['template_id1'],
    success: (res) => {
      if (res['template_id1'] === 'accept') {
        // 调用后端API记录订阅
        this.$api.subscribe.recordSubscribe({
          type: 'countdown',
          related_id: '123',
          subscribe_status: 1,
          subscribe_results: res['template_id1'],
          openid: this.openid
        });
      }
    }
  });
} catch (e) {
  console.error('订阅失败', e);
}
```

## 9. 扩展建议

### 9.1 功能扩展
- 支持批量发送消息
- 添加订阅消息统计功能
- 支持多模板管理
- 实现订阅消息预览功能

### 9.2 性能优化
- 优化access_token缓存机制
- 实现消息发送队列
- 数据库索引优化

### 9.3 安全性
- 加强参数验证
- 添加接口访问频率限制
- 加密敏感数据

### 9.4 使用最佳实践

#### 9.4.1 合理设置发送时间
根据不同业务场景设置合适的发送时间点：
- **倒计时场景**：距离结束前1天、3小时、30分钟、5分钟
- **考试场景**：开始前1天、3小时、30分钟、5分钟，结束前15分钟
- **版本更新场景**：可选择立即发送、特定时间推送或分批推送
- 避免在深夜或用户休息时间发送非紧急消息

**关于一次性订阅与多时间点的说明**：
> 微信小程序一次性订阅消息**每个授权只能发送一次**，文档中设置的多个时间点是指：
> 1. **针对不同用户的灵活选择**：不同用户可能在不同时间订阅，系统会根据订阅时间选择最合适的发送时间点
> 2. **业务逻辑的触发条件**：根据业务场景设置多个可能的发送时机，但每个订阅记录只会触发一次发送
> 3. **用户体验优化**：选择距离当前时间最近的关键时间点发送，避免过早或过晚发送
> 4. **支持重复订阅**：用户可以多次订阅，每次订阅都会生成新的授权记录，支持在不同时间点接收消息

例如：用户在考试开始前2天订阅，系统会选择在开始前1天发送；用户在开始前4小时订阅，系统会选择在开始前3小时发送。

#### 9.4.2 及时发送消息
- 在用户授权有效期（7天）内尽快发送消息，必须在7天前发送
- 避免订阅授权过期失效
- 对于时间敏感的消息，确保在关键时间点前发送
- 实现发送任务监控，及时发现和处理发送失败的情况

#### 9.4.3 提供重新订阅入口
- 在关键页面（如个人中心、设置页面）提供明显的重新订阅按钮
- 当订阅授权过期时，主动提示用户重新订阅
- 提供清晰的订阅价值说明，引导用户重新订阅
- 支持一键重新订阅，简化用户操作流程

#### 9.4.4 保持状态透明
- 向用户清晰展示当前订阅状态
- 在订阅成功/取消时给予明确反馈
- 提供订阅历史查询功能
- 避免频繁打扰用户，尊重用户的订阅选择
- 支持用户自主管理订阅偏好

## 10. 文档更新日志

| 日期 | 版本 | 更新内容 | 更新人 |
|------|------|----------|--------|
| 2026-01-15 | 1.0 | 初始文档 | 精解析题库 |
| 2026-01-15 | 1.1 | 优化文档结构，修复章节编号问题，完善订阅消息发送时间策略 | 精解析题库 |
| 2026-01-15 | 1.2 | 添加使用最佳实践章节，包括合理设置发送时间、及时发送消息、提供重新订阅入口和保持状态透明等内容 | 精解析题库 |
| 2026-01-15 | 1.3 | 添加一次性订阅与多时间点的说明，澄清每个授权只能发送一次的机制 | 精解析题库 |

## 11. 附录

### 11.1 相关文件列表

| 文件路径 | 说明 |
|----------|------|
| c:\Users\44343\jingjiexi\server\app\api\logic\SubscribeLogic.php | 后端订阅逻辑核心文件 |
| c:\Users\44343\jingjiexi\my_client1\subpages\countdown\detail.vue | 倒计时页面 |
| c:\Users\44343\jingjiexi\my_client1\subpages\common\update.vue | 版本更新页面 |
| c:\Users\44343\jingjiexi\my_client1\subpages\examination\examinationContent.vue | 考试内容页面 |
| c:\Users\44343\jingjiexi\server\la_user_subscribe.sql | 数据库表结构 |

### 11.2 参考链接

- [微信小程序订阅消息文档](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/subscribe-message.html)
- [ThinkPHP 8.0 文档](https://www.kancloud.cn/manual/thinkphp8_0/1297883)
- [Vue.js 文档](https://cn.vuejs.org/guide/introduction.html)

### 11.3 联系方式

如有问题或建议，请联系开发团队：
- 邮箱：dev@example.com
- 微信：example_wx
- 电话：12345678900
