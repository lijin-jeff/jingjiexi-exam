## 虚拟列表滑动切换问题修复方案

### 问题根因

**核心问题**：虚拟列表与swiper的current值不同步

1. 用户滑动时，swiper的current变为2（虚拟列表的第三个位置）
2. handleSwiperChange更新swiperCurrentIndex为3
3. swipeCurrent计算属性重新计算为1
4. swiper的current从2变回1，导致滑动效果异常

### 修复方案

#### 1. 重构滑动切换逻辑

```javascript
// 移除对swipeCurrent的依赖
// 直接使用虚拟列表的逻辑处理滑动
```

#### 2. 优化virtualSwiperList和handleSwiperChange的配合

```javascript
// 修改virtualSwiperList，确保滑动后内容正确更新
// 修改handleSwiperChange，处理虚拟列表的特殊逻辑
```

#### 3. 简化滑动切换机制

```javascript
// 移除复杂的防抖和验证逻辑
// 使用更直接的索引计算方式
```

### 修改文件

* `commonQuestion.vue`：

  * 重构handleSwiperChange方法

  * 优化虚拟列表与swiper的配合

  * 确保滑动切换的流畅性

