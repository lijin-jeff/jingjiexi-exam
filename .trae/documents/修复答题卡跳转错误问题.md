## 问题描述

第一次点击57题跳转正确，全部题目加载完成后，再次点击57题跳转到56题。

## 根本原因

`fetchQuestionList` 中的合并逻辑有问题：

```javascript
// 第2018-2035行
for (let i = 0; i < this.simplifiedQuestionList.length; i++) {
  // ...
  // 检查该位置是否已有题目
  if (currentListIndex < currentList.length && 
      currentList[currentListIndex].uid === simplifiedQuestion.uid) {
    mergedList.push(currentList[currentListIndex]);
    currentListIndex++;
  }
}
```

如果 `currentList` 中的题目顺序和 `simplifiedQuestionList` 不一致，`currentListIndex` 会错位，导致 `mergedList` 中题目位置错误。

## 修复方案

### 修改 `fetchQuestionList` 中的合并逻辑（第2018-2035行）

**用 Map 而不是顺序遍历来查找已有题目**：

```javascript
// 原代码
for (let i = 0; i < this.simplifiedQuestionList.length; i++) {
  const simplifiedQuestion = this.simplifiedQuestionList[i];
  
  // 检查该位置是否有新题目需要插入
  while (newQuestionsIndex < questionsWithIndex.length && 
         questionsWithIndex[newQuestionsIndex].targetIndex === i) {
    mergedList.push(questionsWithIndex[newQuestionsIndex].question);
    newQuestionsIndex++;
  }
  
  // 检查该位置是否已有题目
  if (currentListIndex < currentList.length && 
      currentList[currentListIndex].uid === simplifiedQuestion.uid) {
    mergedList.push(currentList[currentListIndex]);
    currentListIndex++;
  }
}

// 新代码
// 步骤1：创建已有题目的 Map，便于快速查找
const existingQuestionsMap = new Map();
for (const question of currentList) {
  if (question.uid) {
    existingQuestionsMap.set(question.uid, question);
  }
}

// 步骤2：按 simplifiedQuestionList 顺序重建列表
for (let i = 0; i < this.simplifiedQuestionList.length; i++) {
  const simplifiedQuestion = this.simplifiedQuestionList[i];
  let added = false;
  
  // 检查该位置是否有新题目需要插入
  while (newQuestionsIndex < questionsWithIndex.length && 
         questionsWithIndex[newQuestionsIndex].targetIndex === i) {
    mergedList.push(questionsWithIndex[newQuestionsIndex].question);
    newQuestionsIndex++;
    added = true;
  }
  
  // 检查该位置是否已有题目（使用 Map 查找）
  if (!added && existingQuestionsMap.has(simplifiedQuestion.uid)) {
    mergedList.push(existingQuestionsMap.get(simplifiedQuestion.uid));
    added = true;
  }
  
  // 如果该位置没有题目，添加占位符
  if (!added) {
    mergedList.push({
      uid: simplifiedQuestion.uid,
      isPlaceholder: true,
      question: null
    });
  }
}
```

## 需要修改的文件

- `c:\Users\44343\jingjiexi\my_client1\subpages\exam\components\commonQuestion.vue`

## 影响范围

- 只影响 `fetchQuestionList` 方法
- 不会影响其他组件或页面
- 修复后 `swiperList` 的索引和 `simplifiedQuestionList` 严格一致

## 验证方式

1. 加载全部题目
2. 点击答题卡57题
3. 观察是否正确跳转到57题
4. 检查 `swiperList` 的长度和顺序是否正确