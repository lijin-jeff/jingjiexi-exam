1. **添加状态变量**：在data中添加`showStatusSelect`和`showSortSelect`变量，用于控制筛选和排序面板的显示状态
2. **创建计算属性**：添加`currentStatusLabel`和`currentSortLabel`，用于显示当前选中的筛选和排序值
3. **修改模板结构**：将当前的筛选和排序标签替换为两个view组件，显示“时间”和“排序方式”
4. **添加选择组件**：引入tn-select组件，分别用于筛选和排序功能
5. **调整事件处理**：修改`onStatusFilterChange`和`onSortChange`函数，适配tn-select组件的回调
6. **优化样式布局**：调整CSS样式，确保新的布局在一行上显示，并且视觉效果协调
7. **保持原有功能**：确保修改后筛选和排序功能正常工作，不影响其他功能

修改文件：

* `c:\Users\44343\jingjiexi\my_client1\subpages\countdown\list.vue`

