将questionCollection.vue文件第986行的错误函数调用`this.loadCollectData(this.questionCllectType);`修改为正确的函数名`this.fetchCollectData(this.questionCllectType);`。

这个错误是因为函数名拼写错误导致的，实际存在的函数是`fetchCollectData`，它用于加载收藏数据，接受type和isLoadMore两个参数，其中type默认为'all'。