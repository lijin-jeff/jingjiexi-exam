### 完善方案

根据数据表 `la_tenant_version_update` 的结构，需要修改服务端代码中的字段名和验证规则，确保与数据表一致。

#### 1. 修改 `TenantVersionUpdateLogic.php`

**当前问题**：使用了错误的字段名（title, author, image, content, click_virtual, is_show），与数据表结构不符

**修改内容**：
- 将 `add` 方法中的字段改为正确的 `version`, `info`, `status`, `release_time`
- 将 `edit` 方法中的字段改为正确的 `version`, `info`, `status`, `release_time`
- 将 `updateStatus` 方法中的 `is_show` 改为 `status`
- 修改 `initialization` 方法，使用正确的字段名

#### 2. 修改 `TenantVersionUpdateValidate.php`

**当前问题**：验证规则使用了错误的字段名（title, is_show）

**修改内容**：
- 修改 `rule` 数组，将 `title` 改为 `version`，`is_show` 改为 `status`
- 添加 `version` 字段的格式验证（x.y.z）
- 添加 `info` 字段的验证规则
- 添加 `release_time` 字段的验证规则（状态为1时必填）
- 修改 `sceneAdd` 和 `sceneEdit` 场景

#### 3. 修改 `TenantVersionUpdateLists.php`

**当前问题**：搜索条件使用了错误的字段名（title, is_show）

**修改内容**：
- 修改 `setSearch` 方法，将 `title` 改为 `version`，`is_show` 改为 `status`
- 确保排序字段与数据表一致

#### 4. 修改 `TenantVersionUpdateController.php`

**修改内容**：
- 确保方法调用参数正确

### 实现步骤

1. 首先修改 `TenantVersionUpdateLogic.php`，更新所有方法的字段名
2. 然后修改 `TenantVersionUpdateValidate.php`，更新验证规则
3. 接着修改 `TenantVersionUpdateLists.php`，更新搜索条件
4. 最后检查 `TenantVersionUpdateController.php`，确保调用正确

### 预期效果

- 服务端代码与数据表结构完全匹配
- 验证规则正确，确保数据完整性
- 搜索条件准确，支持按版本号和状态筛选
- 所有方法调用正常，实现完整的CRUD功能