## 滑动问题修复方案

### 问题1：热区限制问题
- 移除热区限制，或改为更宽松的条件
- 将 `swipeHotZoneRatio` 改为 1.0（全屏可滑动）

### 问题2：垂直滚动回调未实现
- 在组件中设置 `onVerticalScroll` 回调
- 检测到垂直滚动时，临时禁用 swiper 滑动

### 问题3：shouldDisableScroll 未使用
- 修改 swiper 的 `disable-touch` 判断逻辑
- 同时检查 `shouldDisableScroll`

### 问题4：垂直滚动检测阈值
- 降低 `verticalThreshold` 到 5px
- 或调整判定逻辑，优先允许垂直滚动

### 具体修改

1. **swipeHandler.js**：
   - `swipeHotZoneRatio: 0.33 → 1.0`
   - `verticalThreshold: 10 → 5`

2. **commonQuestion.vue**：
   - 设置 `onVerticalScroll` 回调
   - 垂直滚动时禁用 swiper 滑动
   - 滚动结束后恢复