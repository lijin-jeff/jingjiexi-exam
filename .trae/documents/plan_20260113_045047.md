### 修改方案

1. **修改点击事件处理**
   - 将第 60 行的 `@click="tl('/subpages/news/content?id=' + item.id)"` 改为 `@click="handleArticleClick(item)"`
   - 添加 `handleArticleClick` 方法，用于判断文章是否为外部链接并处理跳转

2. **实现外部链接跳转逻辑**
   - 在 `handleArticleClick` 方法中，检查 `item.is_external_link` 是否为 1
   - 如果是外部链接，使用微信小程序 API `wx.openOfficialAccountArticle` 打开外部链接
   - 如果不是外部链接，继续使用原有的导航逻辑跳转到文章详情页

3. **添加错误处理**
   - 为 `wx.openOfficialAccountArticle` API 添加成功和失败回调
   - 失败时显示错误提示

### 实现步骤

1. 修改第 60 行的点击事件绑定
2. 在 `methods` 中添加 `handleArticleClick` 方法
3. 在方法中实现外部链接判断和跳转逻辑
4. 添加适当的错误处理

### 预期效果

- 当文章是外部链接时，点击后调用微信小程序 API 打开外部链接
- 当文章不是外部链接时，继续跳转到文章详情页
- 确保在 API 调用失败时能给出友好提示