1. 解析被取消收藏的question_uid
2. 遍历所有collectData类型，从每个类型的列表中过滤掉被取消收藏的数据
3. 更新收藏统计数据，确保统计信息最新
4. 重置hasMoreData状态，确保后续可以继续加载更多数据

具体修改：
- 将selectedCollectItem转换为数组
- 使用filter方法从collectData的每个类型中移除对应的question_uid
- 调用fetchCollectStats()更新统计数据
- 重置hasMoreData状态

这样修改后，取消收藏成功后，页面会立即更新，而不需要重新调用API获取所有数据，提高了用户体验。