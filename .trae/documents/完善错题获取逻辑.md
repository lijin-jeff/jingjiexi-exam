1. **创建专用错题获取方法**：在QuestionlibLogic.php中创建getErrorQuestionIds()方法，用于从TenantExamQuestionError表获取未被消灭的错题ID
2. **修复逻辑**：
   - 查询TenantExamQuestionError表而非TenantExamExaminationHistory
   - 过滤条件：user_uid匹配，可选library_uid匹配
   - 排除已消灭的错题（通过TenantExamQuestionErrorLog表的new_status=1判断）
   - 返回唯一的错题UID数组
3. **替换现有代码**：
   - 替换第388-403行的错题ID获取逻辑
   - 替换第1520-1534行的错题ID获取逻辑
4. **优化错误处理**：添加适当的异常捕获和日志记录
5. **确保兼容性**：保持原有方法签名不变，确保调用处无需修改

该修复将确保系统从专门的错题表获取错题，并排除已消灭的错题，提高错题获取的准确性和可靠性。