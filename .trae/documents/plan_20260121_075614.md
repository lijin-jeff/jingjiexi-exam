1. 在取消收藏成功后，先清空collectData\['all']，然后再重新获取数据，确保Vue能检测到变化
2. 修改fetchCollectData函数，确保'all'类型的数据能正确更新
3. 优化刷新逻辑，确保只刷新当前显示的数据类型

具体修改：

1. 在取消收藏成功后，先清空collectData\['all']
2. 然后调用fetchCollectData('all')重新获取数据
3. 确保loading状态能正确重置

