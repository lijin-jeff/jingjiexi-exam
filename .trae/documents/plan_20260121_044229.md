按照图鸟UI标准类名规范修改questionRank.vue文件中的类名，具体修改如下：

1. 修改第222行：将`tn-p-20`改为`tn-padding-sm`（使用预定义的sm基准值，对应20rpx）
2. 修改第227行：将`tn-p-30`改为`tn-padding`（使用默认基准值，对应30rpx）
3. 修改第230行：将`tn-items-center`改为`tn-flex-col-center`，将`tn-mb-20`改为`tn-margin-bottom-sm`
4. 修改第243行：将`tn-items-center`改为`tn-flex-col-center`，将`tn-mb-20`改为`tn-margin-bottom-sm`
5. 修改第261行：将`tn-items-center`改为`tn-flex-col-center`，将`tn-mb-20`改为`tn-margin-bottom-sm`

这些修改将确保所有类名符合图鸟UI的标准规范，提高代码的可维护性和一致性。