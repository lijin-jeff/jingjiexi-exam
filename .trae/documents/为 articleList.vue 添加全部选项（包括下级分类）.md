## 修复计划

### 问题分析
用户需要在 `articleList.vue` 文件的分类列表中增加"全部"选项，包括下级分类。当前代码直接将 API 返回的分类数据赋值给 `categoryList`，没有添加"全部"选项。

### 修复方案
修改 `fetchCategoryList()` 方法，在获取到分类数据后：

1. **添加"全部"选项**：在分类列表开头添加一个"全部"选项，值为空字符串
2. **确保数据结构完整**：
   - 为"全部"选项添加 `children` 属性
   - 递归处理所有分类项，确保每个分类项都有 `children` 属性
3. **保持组件配置一致**：确保 "全部" 选项的字段名称与 `tn-select` 组件的配置匹配（`value-name="id"` 和 `label-name="name"`）

### 具体修改步骤

1. **修改 `fetchCategoryList()` 方法**：
   - 在第 279 行附近，修改分类数据处理逻辑
   - 添加递归函数处理分类数据，确保所有分类项都有 `children` 属性
   - 在处理后的分类列表开头添加"全部"选项

2. **验证修改**：
   - 确保 "全部" 选项正确显示在分类列表中
   - 确保 `tn-select` 组件能够正常处理包含"全部"选项的分类数据
   - 确保分类选择功能正常工作

### 预期结果
- 分类列表中会显示"全部"选项作为第一个选项
- "全部"选项会包含所有下级分类
- 分类选择功能正常，选择"全部"选项时能够正确重置分类筛选条件