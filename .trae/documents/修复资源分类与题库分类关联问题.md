## 问题分析

通过分析 `edit.vue` 文件，发现了两个主要问题：

1. **自动填充逻辑不完整**：当前只有在 "add" 模式下才会自动填充题库分类，而 "edit" 模式下不会
2. **禁用状态逻辑**：需要确保选择资源分类后，题库分类正确启用

## 修复计划

1. **修改 `handleChange` 函数**
   - 移除 `mode.value === 'add'` 条件，使自动填充逻辑在所有模式下生效
   - 优化 `findCategory` 函数调用，确保正确查找资源分类
   - 增加调试日志，方便追踪问题

2. **检查禁用状态逻辑**
   - 确保 `formData.category_uid` 正确更新
   - 验证 `disabled` 属性绑定是否正确

3. **优化用户体验**
   - 当资源分类变化时，清空原有的题库分类选择
   - 确保题库分类列表正确加载

## 预期效果

- 选中资源分类时，题库分类自动填充对应的关联值
- 所属题库在选中资源分类后变为可选状态
- 支持新增和编辑两种模式下的自动填充

## 文件修改

- `c:\Users\44343\jingjiexi\tenant\src\views\exam\resource\tenant_resource\edit.vue`
  - 修改 `handleChange` 函数
  - 优化资源分类与题库分类的关联逻辑