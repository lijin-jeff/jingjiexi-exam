1. 在 index.vue 中添加操作类型标识：

   * 新增 `isCopyOperation` 响应式变量

   * 在 `handleEdit` 中设置为 `false`

   * 在 `handleCopy` 中设置为 `true`

   * 向 RadioOption 组件传递 `:is-copy="isCopyOperation"` 属性

2. 更新 RadioOption.vue 组件：

   * 在 Props 接口中添加 `isCopy` 属性

   * 在 `saveForm` 方法中检查 `isCopy` 属性

   * 如果是复制操作，强制调用新增 API，重置 `id` 为 0

   * 如果是编辑操作，使用原有逻辑判断

