### 问题分析

1. **语法错误**：`tenant_resource_category/index.vue` 文件中，el-cascader 组件没有正确关闭，且按钮被错误地放置在组件内部
2. **检查范围**：需要确保所有与题库分类相关的文件都已从多选改为单选
3. **文件列表**：
   - `tenant_resource_category/index.vue` - 资源分类列表页面
   - `tenant_resource_category/edit.vue` - 资源分类编辑页面（已修改）
   - `tenant_resource/index.vue` - 资源列表页面（已修改）
   - `tenant_resource/edit.vue` - 资源编辑页面（已修改）
   - 相关服务端代码（已修改）

### 修复方案

1. **修复语法错误**：
   - 修改 `tenant_resource_category/index.vue` 文件
   - 正确关闭 el-cascader 组件
   - 将按钮移出组件内部

2. **验证所有文件**：
   - 检查所有前端页面的题库分类选择器
   - 确认所有组件都已移除 `multiple` 属性
   - 确认所有 `exam_category_uid` 字段都已从数组改为字符串
   - 确认所有相关方法都已从数组处理改为字符串处理

3. **最终验证**：
   - 确保语法正确
   - 确保功能正常
   - 确保代码风格一致

### 预期效果

- ✅ 修复语法错误
- ✅ 确保所有题库分类选择器都是单选
- ✅ 确保所有相关代码都已适配单选情况
- ✅ 确保服务端代码已适配单选情况