## 同步完善小程序服务端计划

### 1. 目标
确保小程序服务端能够正确处理文章和文章分类的题库分类关联功能，与管理后台和文章功能保持一致。

### 2. 实施步骤

#### 2.1 修改文章列表查询（ArticleLists.php）
- 添加 `exam_category_uid` 到搜索条件中
- 在查询字段中添加 `exam_category_uid`
- 添加 `exam_category_title` 虚拟字段到返回结果
- 在查询中添加 `with(['examCategory'])` 关联查询

#### 2.2 修改文章分类查询（ArticleLogic.php）
- 修改 `cate()` 方法，添加 `exam_category_uid` 参数支持
- 在查询中添加 `exam_category_uid` 筛选条件
- 添加 `exam_category_title` 虚拟字段到返回结果
- 在查询中添加 `with(['examCategory'])` 关联查询

#### 2.3 修改文章控制器（ArticleController.php）
- 确保所有接口支持 `exam_category_uid` 参数

### 3. 关键技术点

#### 3.1 模型关联
确保小程序服务端也使用相同的模型关联：
```php
public function examCategory()
{
    return $this->belongsTo(TenantExamCategory::class, 'exam_category_uid', 'uid');
}
```

#### 3.2 虚拟字段
确保返回题库分类标题：
```php
->append(['exam_category_title'])
```

#### 3.3 搜索条件
添加 `exam_category_uid` 到搜索条件：
```php
return [
    '=' => ['cid', 'exam_category_uid']
];
```

### 4. 实施顺序

1. 首先修改文章列表查询（ArticleLists.php）
2. 然后修改文章分类查询（ArticleLogic.php）
3. 最后修改文章控制器（ArticleController.php）

### 5. 预期效果

- 小程序服务端能够根据 `exam_category_uid` 筛选文章和文章分类
- 文章列表返回包含 `exam_category_title` 字段
- 文章分类列表返回包含 `exam_category_title` 字段
- 与管理后台和文章功能保持一致

### 6. 涉及文件

1. `c:\Users\44343\jingjiexi\server\app\api\lists\article\ArticleLists.php` - 文章列表查询
2. `c:\Users\44343\jingjiexi\server\app\api\logic\ArticleLogic.php` - 文章逻辑处理
3. `c:\Users\44343\jingjiexi\server\app\api\controller\ArticleController.php` - 文章API控制器