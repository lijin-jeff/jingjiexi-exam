修改set.vue文件中的updatedUserInfoEvent方法，添加头像类型判断，仅当avatar是本地文件路径时才调用上传API：

1. 检查info.avatar是否为本地文件路径（判断是否包含"tmp"或"blob"等本地文件标识）
2. 仅当是本地文件路径时，调用apiUploadImage进行上传
3. 否则，直接使用现有URL，跳过上传步骤

这样修改后，当用户只修改用户名而不修改头像时，不会触发不必要的头像上传，从而避免"uploadFile:fail file doesn't exist"错误。