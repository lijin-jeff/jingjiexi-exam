## 将tn-fab的三个按钮改为页面中的标准按钮

### 问题分析
1. 当前使用tn-fab自定义组件，包含三个功能按钮：订阅/取消订阅、分享、新建倒计时
2. 需要将这三个按钮替换为标准按钮，直接显示在页面中
3. 保持原有功能不变，调整布局和样式

### 解决方案
1. 移除tn-fab组件
2. 在页面底部添加标准按钮组
3. 实现三个按钮的功能
4. 调整按钮样式和布局

### 实现步骤
1. 移除tn-fab组件相关代码
2. 在页面底部添加按钮容器
3. 添加三个标准按钮：订阅/取消订阅、分享、新建倒计时
4. 为每个按钮绑定对应的点击事件
5. 设计按钮样式，确保美观和易用
6. 测试按钮功能

### 代码修改
- 文件：`c:\Users\44343\jingjiexi\my_client1\subpages\countdown\detail.vue`
- 移除tn-fab组件
- 添加按钮容器和三个标准按钮
- 绑定按钮点击事件
- 调整样式

### 预期效果
- 移除了悬浮的tn-fab组件
- 页面底部显示三个标准按钮
- 按钮功能与原tn-fab按钮一致
- 页面布局更加简洁明了
- 支持微信小程序分享功能