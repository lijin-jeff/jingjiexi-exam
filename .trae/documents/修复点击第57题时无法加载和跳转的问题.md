## 问题分析

**根本原因**：`activeQuestions` 计算属性只返回当前题目及其前后各一题，当点击第57题时，如果第57题不在当前已加载的题目范围内，`activeQuestions` 就不会包含第57题，导致 swiper 无法正确渲染。

### 具体代码分析：

1. **activeQuestions 计算属性**（第770-797行）：
   - `start = Math.max(0, currentIndex - 1)`
   - `end = Math.min(this.swiperList.length - 1, currentIndex + 1)`
   - 只返回当前题目及其前后各一题

2. **swiper 组件渲染**（第66-69行）：
   - `v-for="(item, index) in activeQuestions"`
   - 只渲染 `activeQuestions` 中的题目

3. **handleQuestionChange 方法**：
   - 虽然加载了第6块题目，但 `activeQuestions` 的计算是基于 `swiperCurrentIndex` 的
   - 如果 `swiperCurrentIndex` 没有及时更新，`activeQuestions` 就不会包含第57题

## 修复方案

### 修改 activeQuestions 计算属性
- **问题**：只返回当前题目及其前后各一题，可能不包含目标题目
- **修复**：修改 `activeQuestions` 计算属性，确保在点击答题卡时能够正确包含目标题目
- **重点**：在 `swiperCurrentIndex` 更新后，确保 `activeQuestions` 包含目标题目

### 修改 handleQuestionChange 方法
- **问题**：`activeQuestions` 计算可能不及时
- **修复**：在设置 `swiperCurrentIndex` 后，强制更新 `activeQuestions`
- **重点**：确保 `activeQuestions` 在跳转前包含目标题目

## 修复步骤

1. **修改 activeQuestions 计算属性**：
   - 增强逻辑，确保在边界情况下也能正确包含目标题目
   - 处理 `swiperCurrentIndex` 更新后的计算

2. **修改 handleQuestionChange 方法**：
   - 在设置 `swiperCurrentIndex` 后，强制更新组件状态
   - 确保 `activeQuestions` 包含目标题目后再跳转

3. **验证修复效果**：
   - 点击第57题，验证是否正确加载和跳转
   - 确认 `activeQuestions` 包含第57题
   - 验证 swiper 正确渲染第57题

## 修复原理

通过修改 `activeQuestions` 计算属性和 `handleQuestionChange` 方法，确保在点击答题卡时，`activeQuestions` 能够正确包含目标题目，从而让 swiper 能够正确渲染和跳转。