## 修改计划

### 1. 修改目标

完善资源分类列表页 `index.vue` 的所属题库分类功能，包括查询和展示功能。

### 2. 修改内容

#### 2.1 分析现有代码

* `index.vue` 中包含资源分类的列表展示和查询功能
* 目前查询条件只有分类名称和显示状态
* 表格中没有展示所属题库分类

#### 2.2 修改点

1. **添加题库分类查询功能**
   - 导入 `apiExamCategoryTree` 接口
   - 添加题库分类数据的响应式变量
   - 添加级联选择器配置
   - 在查询条件中添加 `exam_category_uid` 字段
   - 添加获取题库分类数据的方法

2. **在表格中添加所属题库分类列**
   - 添加新的表格列，展示所属题库分类

3. **修改查询条件**
   - 在 `queryParams` 中添加 `exam_category_uid` 字段

### 3. 具体修改方案

#### 3.1 修改 `index.vue` 文件

* **文件路径**：`c:\Users\44343\jingjiexi\tenant\src\views\exam\resource\tenant_resource_category\index.vue`

* **修改内容**：

  1. 导入 `apiExamCategoryTree` 接口
  2. 添加 `examCategoryList` 响应式变量
  3. 添加 `examCascaderProps` 级联选择器配置
  4. 在 `queryParams` 中添加 `exam_category_uid` 字段
  5. 添加获取题库分类数据的方法
  6. 在查询表单中添加题库分类级联选择器
  7. 在表格中添加所属题库分类列

#### 3.2 代码修改示例

```javascript
// 导入 apiExamCategoryTree
import { apiExamCategoryTree } from '@/api/exam/exam_category'

// 添加响应式变量
const examCategoryList = reactive<any[]>([])

// 添加级联选择器配置
const examCascaderProps = {
    value: 'uid',
    label: 'title',
    children: 'children',
    checkStrictly: true,
    emitPath: false
}

// 修改查询条件
const queryParams = reactive({
    title: '',
    is_show: '',
    exam_category_uid: ''
})

// 添加获取题库分类数据的方法
const fetchExamCategoryList = async () => {
    try {
        const res = await apiExamCategoryTree()
        examCategoryList.length = 0
        examCategoryList.push(...(res || []))
    } catch (error) {
        console.error('获取题库分类失败:', error)
    }
}
fetchExamCategoryList()
```

### 4. 预期效果

1. **查询功能**：列表页可以根据所属题库分类进行查询
2. **展示功能**：表格中显示每条资源分类所属的题库分类
3. **完整功能**：实现了资源分类的所属题库分类的完整功能

### 5. 测试要点

1. 检查题库分类查询是否正常工作
2. 检查表格中是否显示所属题库分类
3. 检查查询条件重置功能是否正常
4. 检查分页功能是否正常

## 实施步骤

1. 修改 `index.vue` 文件，添加题库分类查询功能
2. 在表格中添加所属题库分类列
3. 测试修改后的功能
4. 确保与 `edit.vue` 中的功能保持一致