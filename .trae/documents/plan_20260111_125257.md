## 修复计划

### 1. 修复目标

完善资源分类 `tenant_resource_category` 的所属题库分类功能，参考 `tenant_resource` 的 `apiExamCategoryTree` 调用题库分类功能的实现方式。

### 2. 修复内容

#### 2.1 分析现有代码

* `tenant_resource/edit.vue` 中使用 `apiExamCategoryTree` 获取题库分类数据，并通过级联选择器展示

* `tenant_resource_category/edit.vue` 中有"所属题库"的表单字段，但缺少题库分类的获取和显示逻辑

#### 2.2 修改点

1. **导入 apiExamCategoryTree**：在 `tenant_resource_category/edit.vue` 中导入题库分类API
2. **添加响应式变量**：添加题库分类数据的响应式变量
3. **添加级联选择器配置**：添加题库分类的级联选择器配置
4. **添加数据获取方法**：添加获取题库分类数据的方法
5. **扩展表单数据**：在表单数据中添加 `category_uid` 字段
6. **完善表单验证**：添加 `category_uid` 的验证规则
7. **添加事件处理**：添加级联选择器的变化处理方法

### 3. 具体修改方案

#### 3.1 修改 `tenant_resource_category/edit.vue` 文件

* **文件路径**：`c:\Users\44343\jingjiexi\tenant\src\views\exam\resource\tenant_resource_category\edit.vue`

* **修改内容**：

  1. 在第121行导入 `apiExamCategoryTree`
  2. 添加 `examCategoryList` 响应式变量
  3. 添加 `examCascaderProps` 级联选择器配置
  4. 扩展 `formData` 添加 `category_uid` 字段
  5. 在 `formRules` 中添加 `category_uid` 验证规则
  6. 添加 `handleChangeExamCategory` 方法
  7. 添加 `fetchExamCategoryList` 方法并调用

#### 3.2 代码修改示例

```javascript
// 导入 apiExamCategoryTree
import { apiExamCategoryTree } from '@/api/exam/exam_category'

// 添加响应式变量
const examCategoryList = reactive<any[]>([])

// 添加级联选择器配置
const examCascaderProps = {
    value: 'uid',
    label: 'title',
    children: 'children',
    checkStrictly: true, // 允许选择任意一级
    emitPath: false // 只返回选中节点的值，不返回路径数组
}

// 扩展表单数据
const formData = reactive({
    // 原有字段...
    exam_category_uid: ''
})

// 完善表单验证
const formRules = reactive({
    // 原有规则...
    exam_category_uid: [
        {
            required: true,
            message: '请选择题库分类',
            trigger: ['blur']
        }
    ]
})

// 添加事件处理方法
const handleChangeExamCategory = (value: any) => {
    formData.exam_category_uid = value || ''
}

// 添加数据获取方法
const fetchExamCategoryList = async () => {
    try {
        const res = await apiExamCategoryTree()
        examCategoryList.length = 0
        examCategoryList.push(...(res || []))
    } catch (error) {
        console.error('获取题库分类失败:', error)
    }
}
fetchExamCategoryList()
```

### 4. 预期效果

* 资源分类编辑页面的"所属题库"字段将显示题库分类的级联选择器

* 选择器中会显示完整的题库分类树

* 选择题库分类后，`category_uid` 字段会被正确赋值

* 表单提交时会验证 `category_uid` 是否必填

### 5. 测试要点

* 打开资源分类编辑页面，检查题库分类选择器是否显示

* 检查题库分类数据是否正确加载

* 检查选择题库分类后，数据是否正确保存

* 检查表单验证是否生效

## 实施步骤

1. 修改 `tenant_resource_category/edit.vue` 文件，添加题库分类功能
2. 确保代码语法正确
3. 测试功能是否正常工作

