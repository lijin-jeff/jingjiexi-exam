## 问题分析

### 1. 答题卡没有显示57个题目编号
- **原因**：
  - 题目加载过程中，只加载了部分题目块
  - 题目列表传递给 AnswerCard 组件时不完整
  - 服务端没有实现 `offset` 分页逻辑，导致客户端无法正确加载所有题目块

### 2. 题目块中加载的题目重复
- **根本原因**：
  - **服务端问题**：`c:\Users\44343\jingjiexi\server\app\api\logic\exam\QuestionlibLogic.php` 中的 `orderOptionList` 方法没有处理 `offset` 参数，每次都返回相同的前 `question_count` 个题目
  - **客户端问题**：`fetchQuestionList` 方法中没有去重逻辑，只是简单地将新题目追加到现有列表中
  - **预加载问题**：`preloadNextChunk` 方法可能在短时间内多次触发，导致重复加载相同的题目块

## 修复计划

### 1. 修复服务端分页问题
- **修改文件**：`c:\Users\44343\jingjiexi\server\app\api\logic\exam\QuestionlibLogic.php`
- **修改点**：
  - 在 `orderOptionList` 方法中添加 `offset` 参数支持，实现分页逻辑
  - 修改查询语句，使用 `offset` 和 `limit` 实现正确的分页
  - 确保服务端能根据客户端请求的 `offset` 和 `limit` 返回正确的题目块

### 2. 修复客户端题目重复加载问题
- **修改文件**：`c:\Users\44343\jingjiexi\my_client1\subpages\exam\components\commonQuestion.vue`
- **修改点**：
  - 在 `fetchQuestionList` 方法中添加去重逻辑，使用题目 `uid` 确保不会重复添加相同的题目
  - 优化 `preloadNextChunk` 方法，添加更严格的检查，避免在短时间内多次触发相同的预加载请求
  - 确保 `totalQuestionCount` 被正确设置和使用，避免加载超过总题数的题目

### 3. 确保正确加载所有题目块
- **修改文件**：`c:\Users\44343\jingjiexi\my_client1\subpages\exam\components\commonQuestion.vue`
- **修改点**：
  - 检查 `fetchQuestionList` 方法，确保它能正确加载所有题目块
  - 验证 `totalQuestionCount` 是否被正确设置为实际总题数
  - 确保在加载最后一块题目时，能正确处理不足一个 chunkSize 的情况

### 4. 确保答题卡显示所有题目块
- **修改文件**：`c:\Users\44343\jingjiexi\my_client1\subpages\exam\components\commonQuestion.vue`
- **修改点**：
  - 确保传递给 AnswerCard 组件的 `swiperList` 包含所有已加载的题目
  - 检查题目加载完成后的逻辑，确保 AnswerCard 能收到完整的题目列表

### 5. 验证修复效果
- **测试点**：
  - 验证服务端是否能正确处理 `offset` 参数，返回不同的题目块
  - 验证客户端是否能正确加载所有题目块
  - 验证题目列表中是否没有重复的题目
  - 验证答题卡是否显示所有题目块的编号
  - 验证滑动切换题目时是否正常，没有重复加载

## 预期结果
- 服务端能正确处理 `offset` 参数，实现分页逻辑
- 客户端能正确加载所有题目块，没有重复的题目
- 答题卡显示所有题目块的编号
- 题目加载和滑动切换功能正常
- 性能和用户体验得到改善