### 问题分析
1. **resourceList.vue 问题**
   - 第69-72行：引用不存在的字段 `it.label` 和 `it.color`
   - 第66-67行：空视图，没有实际内容
   - 第182-186行：调用不存在的API接口 `apiResourceCategoryList`

2. **content.vue 问题**
   - 第592-610行：调用不存在的预览页面 `/subpages/resource/preview`
   - 第186-206行：客服二维码模态框使用硬编码配置
   - 第237-245行：激励视频广告组件使用硬编码的ad-unit-id
   - 第328-332行：`getRandomAdId` 方法使用硬编码的广告ID数组

3. **缺少页面和功能**
   - 缺少资源预览页面 `/subpages/resource/preview`
   - 缺少统一的广告配置管理

### 修复方案
1. **修改 resourceList.vue**
   - 移除不存在的字段引用，改为使用实际存在的字段
   - 移除空视图
   - 修复API接口调用

2. **修改 content.vue**
   - 移除不存在的预览页面调用
   - 使用全局配置替换硬编码的广告ID
   - 优化客服二维码模态框的配置获取

3. **新增资源预览页面**
   - 创建 `/subpages/resource/preview.vue` 页面
   - 实现资源预览功能

4. **优化代码结构**
   - 统一广告配置管理
   - 优化错误处理
   - 提高代码的可维护性

### 预期效果
- 修复前端页面的显示错误
- 确保所有功能正常运行
- 提高代码的健壮性和可维护性
- 优化用户体验