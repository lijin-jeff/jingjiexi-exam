### 完善方案

根据 `tenant_user_integral_log` 的实现方式，需要对 `version_update` 进行以下完善：

#### 1. 修改 index.vue

**功能完善**：

* 添加批量删除功能

* 支持添加和编辑弹窗

* 完善表格列和操作按钮

* 确保使用正确的 API 调用

**实现要点**：

* 使用 `el-card` 布局

* 添加搜索表单

* 表格支持批量选择

* 使用 `edit-popup` 组件处理添加和编辑

* 使用 `usePaging` hook 处理分页

* 完善表格操作列

#### 2. 修改 edit.vue

**功能完善**：

* 确保表单字段与数据库表匹配

* 完善表单验证规则

* 支持添加和编辑两种模式

* 确保 API 调用正确

**实现要点**：

* 使用 `Popup` 组件

* 支持异步提交

* 表单字段与数据库表匹配

* 完善表单验证规则

* 定义 `open` 和 `setFormData` 方法供父组件调用

#### 3. API 调用

确保使用正确的 API 调用：

* `versionUpdateAdd` - 添加版本更新

* `versionUpdateEdit` - 编辑版本更新

* `versionUpdateDelete` - 删除版本更新

* `versionUpdateDetail` - 获取版本更新详情

* `versionUpdateLists` - 获取版本更新列表

### 实现步骤

1. 修改 `index.vue`，添加批量删除和弹窗功能
2. 修改 `edit.vue`，确保表单字段和验证规则正确
3. 确保 API 调用与组件功能匹配
4. 测试添加、编辑、删除功能

### 预期效果

* 支持批量删除版本更新

* 支持弹窗方式添加和编辑版本更新

* 表单字段与数据库表匹配

* 验证规则完善

* API 调用正确

* 用户操作流畅

