## 整合方案

1. **保留现有结构**：保持`UserSubscribeController`类的现有命名空间和继承关系
2. **添加新方法**：将用户提供的三个新方法整合到现有类中
3. **调整命名空间和继承**：确保新代码使用项目的命名空间和继承关系
4. **保持现有方法**：保留现有的`subscribe`和`status`方法

## 具体修改点

1. **修改** `c:\Users\44343\jingjiexi\server\app\api\controller\UserSubscribeController.php` 文件：
   - 添加 `use app\model\UserSubscribe` 导入
   - 添加 `recordSubscribe` 方法
   - 添加 `checkCanPush` 方法
   - 添加 `updatePushStatus` 方法
   - 确保所有方法使用 `Json` 返回类型

## 预期效果

- 保留现有的统一订阅功能
- 添加新的模板订阅记录和推送状态管理功能
- 代码符合项目的命名空间和继承关系
- 所有方法返回格式统一

