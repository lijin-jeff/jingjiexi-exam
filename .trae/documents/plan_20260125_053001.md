## 1. 修复前端组件事件

### 修改文件
- `c:\Users\44343\jingjiexi\tenant\src\views\consumer\lists\detail.vue`

### 修改内容
- 将 `el-switch` 组件的 `@confirm` 事件改为 `@change` 事件，因为 `el-switch` 组件没有 `@confirm` 事件
- 修复 `handleEdit` 方法的调用，确保参数正确

## 2. 实现消费者列表的管理员状态更新

### 修改文件
- `c:\Users\44343\jingjiexi\tenant\src\views\consumer\lists\index.vue`

### 修改内容
- 在 `handleAdminChange` 方法中调用 API 更新管理员状态
- 确保 API 调用参数正确

## 3. 确保前端 API 正确调用

### 修改文件
- `c:\Users\44343\jingjiexi\tenant\src\api\consumer.ts`

### 修改内容
- 确保 `userEdit` API 能够正确处理 `is_admin` 字段的更新

## 4. 验证后端逻辑

### 修改文件
- `c:\Users\44343\jingjiexi\server\app\tenantapi\logic\user\UserLogic.php`

### 修改内容
- 确认 `setUserInfo` 方法能够正确处理 `is_admin` 字段的更新
- 确认 `detail` 方法返回 `is_admin` 字段

## 5. 验证数据库字段

### 修改内容
- 确保 `is_admin` 字段已正确添加到 `la_user` 表
- 字段类型：TINYINT(1)
- 默认值：0
- 注释：是否为管理员，0否1是

## 预期效果

1. 在消费者列表中可以通过开关切换用户的管理员状态
2. 在用户详情页中可以通过开关切换用户的管理员状态
3. 开关状态变化时，会调用 API 更新到服务器
4. 服务器能够正确处理管理员状态的更新
5. 页面刷新后，管理员状态显示正确

这个计划将确保管理员状态调整功能能够正常工作，包括前端交互和后端处理。