### 问题分析

1. **题目标题显示问题**：
   - 当前题目标题容器始终显示，未根据笔记类型进行条件判断
   - 可能存在字段名不匹配问题，导致题目标题无法正确显示

2. **标签切换问题**：
   - 标签切换逻辑已存在，但可能存在API调用参数或数据处理问题
   - 不同类型的评论（题目笔记、文章评论、资源评论）需要分别展示

### 修复方案

1. **完善题目标题显示**：
   - 在题目标题容器上添加 `v-if` 条件，仅当笔记类型为题目笔记（`note.type === 1`）时显示
   - 检查并确保使用正确的字段名获取题目标题
   - 为不同类型的笔记添加适当的展示内容

2. **优化标签切换功能**：
   - 确保标签切换时正确重置查询参数和数据列表
   - 验证API调用时 `type` 参数是否正确传递
   - 为不同类型的笔记添加适当的UI展示

### 实施步骤

1. **修改题目标题显示逻辑**：
   - 在题目标题容器上添加 `v-if="note.type === 1"`
   - 检查并调整题目标题的字段名
   - 添加其他类型笔记的展示逻辑（如文章标题、资源标题）

2. **优化标签切换功能**：
   - 确保 `onInnerTabChange` 方法正确重置查询参数
   - 验证 `loadNoteList` 方法中API调用参数是否正确
   - 为不同类型的笔记添加不同的UI展示

### 预期效果

1. **题目标题显示**：
   - 仅题目笔记显示题目标题
   - 文章评论显示文章标题
   - 资源评论显示资源标题
   - 所有标题正确显示，无"无标题"情况

2. **标签切换功能**：
   - 点击不同标签，正确加载对应类型的评论列表
   - 切换标签时，数据列表正确重置和刷新
   - 加载状态正确显示

### 代码修改点

1. **模板部分**：
   - 第118行附近：修改题目标题容器，添加条件判断
   - 添加其他类型笔记的展示模板

2. **脚本部分**：
   - 确保 `onInnerTabChange` 方法正确执行
   - 验证 `loadNoteList` 方法中API调用参数
   - 可能需要调整数据处理逻辑

### 注意事项

1. 确保API返回的数据中包含正确的笔记类型和标题字段
2. 注意不同类型笔记的数据结构差异
3. 保持UI一致性，不同类型笔记的展示风格统一
4. 确保加载状态和空状态正确处理