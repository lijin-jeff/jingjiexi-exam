1. **修改查询参数类型**：将 `queryParams.exam_category_uid` 从字符串类型改为数组类型
2. **组件配置修改**：在 `el-cascader` 组件上添加 `multiple` 和 `collapse-tags` 属性
3. **调整重置逻辑**：确保 `resetParams` 方法能正确处理数组类型的查询参数
4. **检查后端兼容性**：确保后端 API 能处理多选查询参数
5. **测试功能完整性**：验证查询、重置、分页等功能正常工作

