## 问题分析

根据提供的代码片段，当前存在的问题是：

1. 页面有两个选择器：
   - 所属题库（el-cascader）：绑定到`formData.exam_category_uid`
   - 上级分类（el-tree-select）：绑定到`formData.parent_uid`，数据源是`parentList`

2. 但这两个选择器之间没有关联，选择所属题库后，上级分类不会自动更新为该题库下的分类

## 解决方案

1. **添加关联逻辑**：当选择所属题库时，根据选中的`exam_category_uid`获取关联的分类列表，更新`parentList`

2. **实现步骤**：
   - 在`handleChangeExamCategory`方法中，根据选中的题库ID获取关联分类
   - 调用API获取该题库下的分类列表
   - 将返回的分类列表赋值给`parentList`
   - 清空之前选中的上级分类，避免数据不一致

3. **关键修改点**：
   - 在`handleChangeExamCategory`方法中添加获取关联分类的逻辑
   - 确保API接口支持根据题库ID获取分类列表
   - 处理分类数据的格式转换，确保符合el-tree-select的要求

## 实施步骤

1. 查看`handleChangeExamCategory`方法的现有实现
2. 在该方法中添加根据`exam_category_uid`获取关联分类的逻辑
3. 确保API调用正确，返回数据格式符合要求
4. 测试选择题库后上级分类是否正确更新

## 预期效果

- 用户选择所属题库后，上级分类选择器只显示该题库下的分类
- 之前选中的上级分类会被清空，避免数据不一致
- 分类列表会根据选择的题库动态更新

## 代码修改方向

1. 在`handleChangeExamCategory`方法中添加：
   ```javascript
   // 根据选中的题库ID获取关联分类
   this.fetchCategoryByExamId(exam_category_uid)
   ```

2. 实现`fetchCategoryByExamId`方法：
   ```javascript
   fetchCategoryByExamId(examId) {
     // 调用API获取该题库下的分类列表
     this.$api.getCategoryByExamId(examId).then(res => {
       this.parentList = res.data
       // 清空之前选中的上级分类
       this.formData.parent_uid = ''
     })
   }
   ```

3. 确保初始化时根据已有`exam_category_uid`获取分类

## 需要注意的问题

1. 确保API接口支持根据题库ID获取分类列表
2. 处理API返回数据的格式，确保符合el-tree-select的要求
3. 处理清空上级分类时的用户体验
4. 确保编辑已有数据时的正确初始化