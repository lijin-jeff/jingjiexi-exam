# 精解析答题系统 - 部署说明文档

> 版本：v1.0.0  
> 更新日期：2025-12-27  
> 适用环境：Linux/Windows Server

---

## 📋 目录

1. [系统架构概述](#系统架构概述)
2. [服务器环境要求](#服务器环境要求)
3. [部署前准备](#部署前准备)
4. [后端部署](#后端部署)
5. [前端部署](#前端部署)
6. [小程序部署](#小程序部署)
7. [定时任务配置](#定时任务配置)
8. [域名与SSL配置](#域名与ssl配置)
9. [性能优化建议](#性能优化建议)
10. [常见问题处理](#常见问题处理)

---

## 系统架构概述

本系统采用**前后端分离**架构，包含以下四个部分：

```
tutu/
├── server/          # 后端服务（ThinkPHP 8.0）
├── platform/        # 平台管理端（Vue 3 + TypeScript + Vite）
├── tenant/          # 租户管理端（Vue 3 + TypeScript + Vite）
└── user_client1/    # 用户小程序端（UniApp）
```

### 技术栈
- **后端**：ThinkPHP 8.0、PHP 8.0+、MySQL 5.7+、Redis
- **前端**：Vue 3、TypeScript、Vite、Element Plus、Tailwind CSS、Pinia
- **小程序**：UniApp、图鸟UI

---

## 服务器环境要求

### 硬件配置建议

| 环境 | CPU | 内存 | 磁盘 | 带宽 |
|------|-----|------|------|------|
| 测试环境 | 2核 | 4GB | 40GB | 3Mbps |
| 生产环境 | 4核+ | 8GB+ | 100GB+ | 10Mbps+ |

### 软件环境

#### 必需环境
- **操作系统**：Linux（推荐 CentOS 7+/Ubuntu 18.04+）或 Windows Server 2016+
- **Web服务器**：Nginx 1.18+ 或 Apache 2.4+
- **PHP**：≥ 8.0（推荐 PHP 8.1）
- **MySQL**：≥ 5.7（推荐 MySQL 8.0）
- **Redis**：≥ 5.0
- **Node.js**：≥ 16.x（用于前端构建）
- **Composer**：2.x

#### PHP扩展要求
```bash
# 必需扩展
php-fpm
php-mysql (PDO_MYSQL)
php-redis
php-curl
php-zip
php-iconv
php-mbstring
php-json
php-xml
php-gd

# 推荐扩展
php-opcache
php-intl
```

---

## 部署前准备

### 1. 获取源码

```bash
# 克隆或上传代码到服务器
cd /www/wwwroot/
git clone <repository_url> tutu
# 或使用 FTP/SFTP 上传压缩包后解压
```

### 2. 安装依赖工具

#### 安装 Composer
```bash
# Linux
curl -sS https://getcomposer.org/installer | php
mv composer.phar /usr/local/bin/composer

# 验证安装
composer --version
```

#### 安装 Node.js
```bash
# 使用 nvm (推荐)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 16
nvm use 16

# 或使用包管理器
# CentOS
yum install nodejs npm

# Ubuntu
apt-get install nodejs npm

# 验证安装
node -v
npm -v
```

### 3. 创建数据库

```sql
-- 登录 MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE dt_allpp_cn CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建数据库用户（生产环境建议使用独立用户）
CREATE USER 'dt_allpp_cn'@'localhost' IDENTIFIED BY '您的强密码';
GRANT ALL PRIVILEGES ON dt_allpp_cn.* TO 'dt_allpp_cn'@'localhost';
FLUSH PRIVILEGES;

-- 导入基础数据表
USE dt_allpp_cn;
SOURCE /www/wwwroot/tutu/server/public/install/db/dt_allpp_cn.sql;

-- 导入功能增强脚本（按顺序执行）
SOURCE /www/wwwroot/tutu/server/question_like_collect_enhancement.sql;
SOURCE /www/wwwroot/tutu/server/user_chapter_statistics_schema.sql;
SOURCE /www/wwwroot/tutu/server/user_chapter_statistics_migration.sql;
SOURCE /www/wwwroot/tutu/server/message_center_schema.sql;
SOURCE /www/wwwroot/tutu/server/add_ranking_type_column.sql;
SOURCE /www/wwwroot/tutu/server/insert_ranking_crontab.sql;
SOURCE /www/wwwroot/tutu/server/fix_exam_type_null.sql;
SOURCE /www/wwwroot/tutu/server/public/install/db/ranking_performance_optimize.sql;

-- 退出
EXIT;
```

### 4. 配置 Redis

```bash
# 启动 Redis
systemctl start redis
systemctl enable redis

# 设置 Redis 密码（生产环境必需）
redis-cli
CONFIG SET requirepass "您的Redis密码"
CONFIG REWRITE
exit
```

---

## 后端部署

### 1. 安装 PHP 依赖

```bash
cd /www/wwwroot/tutu/server

# 安装依赖（生产环境）
composer install --no-dev --optimize-autoloader

# 如果是开发环境
# composer install
```

### 2. 配置环境变量

```bash
# 复制配置文件模板
cp .example.env .env

# 编辑配置文件
vim .env
```

**关键配置项**：

```ini
[APP]
DEFAULT_TIMEZONE = "Asia/Shanghai"
# ⚠️ 重要：改为正式域名，结尾必须有 /
URL="https://your-domain.com/"

[DATABASE]
TYPE = "mysql"
HOSTNAME = "127.0.0.1"
DATABASE = "dt_allpp_cn"
USERNAME = "dt_allpp_cn"
PASSWORD = "您的数据库密码"
HOSTPORT = "3306"
CHARSET = "utf8mb4"
# ⚠️ 生产环境必须设为 0
DEBUG = "0"
PREFIX = "la_"

[LANG]
default_lang = "zh-cn"

[PROJECT]
UNIQUE_IDENTIFICATION = "188e"
# ⚠️ 修改默认密码
DEFAULT_PASSWORD = "YourSecurePassword123!"
# ⚠️ 生产环境必须为空
DEMO_ENV = ""
HTTP_HOST = "your-domain.com"

[REDIS]
HOST="127.0.0.1"
DB=0
# ⚠️ 设置 Redis 密码
AUTH="您的Redis密码"
PORT="6379"
```

### 3. 设置文件权限

```bash
# 进入项目目录
cd /www/wwwroot/tutu/server

# 设置运行时目录权限
chmod -R 755 runtime
chown -R www:www runtime

# 设置上传目录权限
chmod -R 755 public/uploads
chown -R www:www public/uploads

# 设置其他目录权限
find . -type d -exec chmod 755 {} \;
find . -type f -exec chmod 644 {} \;

# 确保入口文件可执行
chmod 644 public/index.php
```

### 4. 配置 Web 服务器

#### Nginx 配置

创建配置文件 `/etc/nginx/conf.d/tutu.conf`：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 强制跳转 HTTPS（配置 SSL 后启用）
    # return 301 https://$server_name$request_uri;
    
    root /www/wwwroot/tutu/server/public;
    index index.php index.html;
    
    # 字符集
    charset utf-8;
    
    # 访问日志
    access_log /var/log/nginx/tutu_access.log;
    error_log /var/log/nginx/tutu_error.log;
    
    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
    
    # 静态文件缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # URL 重写（ThinkPHP 路由）
    location / {
        if (!-e $request_filename) {
            rewrite ^(.*)$ /index.php?s=$1 last;
            break;
        }
    }
    
    # PHP 处理
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php-fpm/php-fpm.sock;
        # 或使用 TCP 方式
        # fastcgi_pass 127.0.0.1:9000;
        
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        
        # 超时设置
        fastcgi_connect_timeout 300s;
        fastcgi_send_timeout 300s;
        fastcgi_read_timeout 300s;
    }
    
    # 文件上传大小限制
    client_max_body_size 100M;
}

# HTTPS 配置（配置 SSL 证书后使用）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     
#     ssl_certificate /path/to/your/cert.pem;
#     ssl_certificate_key /path/to/your/key.pem;
#     ssl_session_timeout 5m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
#     ssl_prefer_server_ciphers on;
#     
#     # ... 其他配置同上
# }
```

#### Apache 配置（如使用 Apache）

创建或修改 `.htaccess`（已在 `server/public/.htaccess` 中）：

```apache
<IfModule mod_rewrite.c>
  Options +FollowSymlinks -Multiviews
  RewriteEngine On

  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ index.php/$1 [QSA,PT,L]
</IfModule>
```

### 5. 重启服务

```bash
# 重启 Nginx
systemctl restart nginx

# 或重启 Apache
systemctl restart httpd

# 重启 PHP-FPM
systemctl restart php-fpm

# 重启 Redis
systemctl restart redis
```

### 6. 测试后端

```bash
# 测试 API 是否正常
curl https://your-domain.com/api/index/index

# 应返回 JSON 数据
```

---

## 前端部署

### 1. Platform 管理端部署

#### 步骤 1：配置生产环境

```bash
cd /www/wwwroot/tutu/platform

# 创建生产环境配置文件
cp .env.production.example .env.production

# 编辑配置
vim .env.production
```

**配置内容**：

```bash
NODE_ENV = 'production'
# ⚠️ 改为正式 API 地址
VITE_APP_BASE_URL='https://your-domain.com/'
```

#### 步骤 2：安装依赖并构建

```bash
# 安装依赖
npm install

# 或使用 pnpm（更快）
# npm install -g pnpm
# pnpm install

# 执行构建
npm run build

# 构建完成后，文件会自动复制到 server/public/platform/
```

#### 步骤 3：验证部署

```bash
# 检查文件是否生成
ls -la /www/wwwroot/tutu/server/public/platform/

# 应包含以下文件：
# - index.html
# - assets/ (包含 JS、CSS、图片等)
```

#### 访问测试

浏览器访问：`https://your-domain.com/platform/`

### 2. Tenant 租户端部署

#### 步骤 1：配置生产环境

```bash
cd /www/wwwroot/tutu/tenant

# 创建生产环境配置文件
cp .env.production.example .env.production

# 编辑配置
vim .env.production
```

**配置内容**：

```bash
NODE_ENV = 'production'
VITE_APP_BASE_URL='https://your-domain.com/'
```

#### 步骤 2：安装依赖并构建

```bash
# 安装依赖
npm install

# 执行构建
npm run build

# 构建完成后，文件会自动复制到 server/public/tenant/
```

#### 步骤 3：验证部署

```bash
# 检查文件是否生成
ls -la /www/wwwroot/tutu/server/public/tenant/
```

#### 访问测试

浏览器访问：`https://your-domain.com/tenant/`

---

## 小程序部署

### 1. 配置 API 地址

修改小程序配置文件中的 API 地址：

```bash
cd /www/wwwroot/tutu/user_client1

# 查找并修改所有 API 配置
# 通常在 utils/request.js 或 config.js 中
```

**示例配置**：

```javascript
// utils/request.js
const baseURL = 'https://your-domain.com/'
```

### 2. 修改 manifest.json

```bash
vim manifest.json
```

**关键配置**：

```json
{
    "name": "精解析答题系统",
    "appid": "__UNI__XXXXXXXX",  // ⚠️ 改为正式小程序 AppID
    "versionName": "1.0.0",      // 版本号
    "versionCode": "100",        // 版本代码
    // ... 其他配置
}
```

### 3. 使用 HBuilderX 发行

1. 打开 HBuilderX
2. 打开项目 `user_client1`
3. 菜单：发行 → 小程序-微信
4. 填写相关信息后点击发行
5. 等待编译完成

### 4. 微信小程序配置

#### 登录微信公众平台

https://mp.weixin.qq.com

#### 配置服务器域名

**设置 → 开发设置 → 服务器域名**

添加以下域名：

```
request合法域名：
https://your-domain.com

uploadFile合法域名：
https://your-domain.com

downloadFile合法域名：
https://your-domain.com
```

#### 提交审核

1. 使用微信开发者工具上传代码
2. 登录微信公众平台
3. 版本管理 → 开发版本 → 提交审核
4. 填写版本说明和测试账号
5. 等待审核通过后发布

---

## 定时任务配置

### 1. 排行榜更新任务

#### 使用宝塔面板（推荐）

1. 登录宝塔面板
2. 计划任务 → 添加任务
3. 配置任务：

```
任务类型：Shell脚本
任务名称：排行榜统计更新
执行周期：每天 00:00
脚本内容：
cd /www/wwwroot/tutu/server && php think crontab handle rankingStatistics
```

#### 使用系统 Crontab

```bash
# 编辑 crontab
crontab -e

# 添加任务（每天凌晨0点执行）
0 0 * * * cd /www/wwwroot/tutu/server && php think crontab handle rankingStatistics >> /var/log/ranking_cron.log 2>&1
```

### 2. 消息推送任务

```bash
# 宝塔面板配置
任务类型：Shell脚本
任务名称：消息中心推送
执行周期：每小时
脚本内容：
cd /www/wwwroot/tutu/server && php think crontab handle messagePush

# 或使用系统 Crontab
0 * * * * cd /www/wwwroot/tutu/server && php think crontab handle messagePush >> /var/log/message_cron.log 2>&1
```

### 3. 验证定时任务

```bash
# 手动执行测试
cd /www/wwwroot/tutu/server
php think crontab handle rankingStatistics

# 查看日志
tail -f runtime/log/*.log
```

**详细配置请参考**：
- `排行榜定时任务配置说明.md`
- `消息通知定时任务配置指南.md`
- `宝塔面板定时任务配置指南.md`

---

## 域名与SSL配置

### 1. 域名解析

在域名服务商处添加 DNS 解析：

```
类型    主机记录    记录值              TTL
A       @          服务器公网IP         600
A       www        服务器公网IP         600
```

### 2. SSL 证书申请

#### 方式一：使用宝塔面板（最简单）

1. 登录宝塔面板
2. 网站 → 选择站点 → SSL
3. 选择 Let's Encrypt 免费证书
4. 点击申请，等待自动配置

#### 方式二：使用 Certbot

```bash
# 安装 Certbot
yum install certbot python3-certbot-nginx  # CentOS
apt-get install certbot python3-certbot-nginx  # Ubuntu

# 申请证书
certbot --nginx -d your-domain.com -d www.your-domain.com

# 自动续期（添加到 crontab）
0 3 * * * certbot renew --quiet
```

### 3. 配置 HTTPS

修改 Nginx 配置，启用之前注释的 HTTPS 部分：

```bash
vim /etc/nginx/conf.d/tutu.conf

# 取消 HTTPS server 块的注释
# 并修改证书路径

# 重启 Nginx
systemctl restart nginx
```

### 4. 强制 HTTPS

在 HTTP server 块中添加：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

---

## 性能优化建议

### 1. PHP 优化

#### 启用 OPcache

编辑 `php.ini`：

```ini
[opcache]
opcache.enable=1
opcache.enable_cli=1
opcache.memory_consumption=128
opcache.interned_strings_buffer=8
opcache.max_accelerated_files=10000
opcache.revalidate_freq=60
opcache.fast_shutdown=1
```

#### 调整 PHP-FPM

编辑 `/etc/php-fpm.d/www.conf`：

```ini
pm = dynamic
pm.max_children = 50
pm.start_servers = 5
pm.min_spare_servers = 5
pm.max_spare_servers = 35
pm.max_requests = 500
```

### 2. MySQL 优化

编辑 `/etc/my.cnf`：

```ini
[mysqld]
# 连接数
max_connections = 500

# 缓冲池大小（建议设置为物理内存的 70-80%）
innodb_buffer_pool_size = 4G

# 查询缓存
query_cache_type = 1
query_cache_size = 64M

# 慢查询日志
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 2
```

### 3. Redis 优化

编辑 `redis.conf`：

```conf
# 最大内存
maxmemory 2gb

# 内存淘汰策略
maxmemory-policy allkeys-lru

# 持久化（根据需要选择）
save ""  # 关闭 RDB
appendonly yes  # 启用 AOF
```

### 4. Nginx 优化

```nginx
# 工作进程数（等于 CPU 核心数）
worker_processes auto;

# 事件模块
events {
    worker_connections 4096;
    use epoll;
}

http {
    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript 
               application/json application/javascript application/xml+rss;
    
    # 缓冲区大小
    client_body_buffer_size 128k;
    client_max_body_size 100m;
    
    # 超时设置
    keepalive_timeout 65;
    send_timeout 30;
    
    # 开启文件缓存
    open_file_cache max=10000 inactive=30s;
    open_file_cache_valid 60s;
    open_file_cache_min_uses 2;
}
```

### 5. 前端优化

#### 启用 CDN（可选）

修改前端配置，将静态资源上传到 CDN：

```javascript
// vite.config.ts
export default defineConfig({
    build: {
        rollupOptions: {
            output: {
                // 代码分割
                manualChunks(id) {
                    if (id.includes('node_modules')) {
                        return id.toString().split('node_modules/')[1].split('/')[0].toString()
                    }
                }
            }
        }
    }
})
```

---

## 常见问题处理

### 1. 权限问题

**问题**：无法写入日志或上传文件

**解决**：

```bash
# 检查目录所有者
ls -la /www/wwwroot/tutu/server/runtime
ls -la /www/wwwroot/tutu/server/public/uploads

# 修改所有者为 Web 服务器用户
chown -R www:www runtime
chown -R www:www public/uploads

# 设置权限
chmod -R 755 runtime
chmod -R 755 public/uploads
```

### 2. 404 错误

**问题**：访问前端页面显示 404

**解决**：

检查 Nginx 配置中的 `root` 路径是否正确：

```nginx
# 应该指向 server/public 目录
root /www/wwwroot/tutu/server/public;
```

### 3. API 跨域问题

**问题**：前端请求 API 出现跨域错误

**解决**：

在 Nginx 中添加 CORS 头：

```nginx
location /api {
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS';
    add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
    
    if ($request_method = 'OPTIONS') {
        return 204;
    }
}
```

### 4. 数据库连接失败

**问题**：提示 "Connection refused"

**解决**：

```bash
# 检查 MySQL 是否运行
systemctl status mysql

# 检查端口是否监听
netstat -tuln | grep 3306

# 测试数据库连接
mysql -h 127.0.0.1 -u dt_allpp_cn -p

# 检查防火墙
firewall-cmd --list-all
```

### 5. Redis 连接失败

**问题**：提示 "Connection refused" 或 "Auth failed"

**解决**：

```bash
# 检查 Redis 是否运行
systemctl status redis

# 测试连接
redis-cli
AUTH 您的Redis密码
PING

# 检查配置文件
vim /etc/redis.conf
# 确认 bind 127.0.0.1 和 requirepass
```

### 6. PHP 内存不足

**问题**：提示 "Allowed memory size exhausted"

**解决**：

编辑 `php.ini`：

```ini
memory_limit = 256M
```

重启 PHP-FPM：

```bash
systemctl restart php-fpm
```

### 7. 文件上传大小限制

**问题**：上传大文件失败

**解决**：

修改三处配置：

**PHP 配置（php.ini）**：

```ini
upload_max_filesize = 100M
post_max_size = 100M
```

**Nginx 配置**：

```nginx
client_max_body_size 100M;
```

**PHP-FPM 配置**：

```ini
php_value[upload_max_filesize] = 100M
php_value[post_max_size] = 100M
```

重启所有服务。

### 8. 租户ID不匹配

**问题**：用户操作失败，日志显示 "租户ID不匹配"

**解决**：

参考 `DIAGNOSIS_REPORT.md` 文档，检查：

1. 用户表 `la_user` 的 `tenant_id` 字段
2. API 请求的 `request()->tenantId` 值
3. 确保两者匹配

### 9. 时间戳字段溢出

**问题**：提示 "Out of range value"

**解决**：

检查时间戳字段类型：
- 10位秒：使用 `int(10)`
- 13位毫秒：使用 `bigint(13)`

修改表结构：

```sql
ALTER TABLE table_name MODIFY COLUMN update_time BIGINT(13);
```

---

## 部署检查清单

部署完成后，请按以下清单逐项检查：

### 后端检查
- [ ] PHP 版本 ≥ 8.0
- [ ] Composer 依赖已安装
- [ ] `.env` 配置正确
- [ ] 数据库连接成功
- [ ] Redis 连接成功
- [ ] 文件权限正确（runtime、uploads）
- [ ] Web 服务器配置正确
- [ ] API 接口可访问
- [ ] 日志可正常写入

### 前端检查
- [ ] Platform 管理端可访问（`/platform/`）
- [ ] Tenant 租户端可访问（`/tenant/`）
- [ ] API 请求正常
- [ ] 登录功能正常
- [ ] 静态资源加载正常

### 小程序检查
- [ ] AppID 已配置
- [ ] API 地址已修改
- [ ] 服务器域名已配置
- [ ] 代码已上传并提审

### 定时任务检查
- [ ] 排行榜任务已配置
- [ ] 消息推送任务已配置
- [ ] 手动执行测试通过

### 安全检查
- [ ] DEBUG 模式已关闭
- [ ] DEMO_ENV 为空
- [ ] 默认密码已修改
- [ ] Redis 密码已设置
- [ ] SSL 证书已配置
- [ ] HTTPS 强制跳转已启用
- [ ] 敏感文件不可访问（.env、composer.json等）

### 性能检查
- [ ] OPcache 已启用
- [ ] Gzip 压缩已启用
- [ ] 静态资源缓存已配置
- [ ] 数据库索引已建立
- [ ] Redis 缓存正常工作

---

## 技术支持

如遇到部署问题，请按以下步骤排查：

1. **查看错误日志**
   ```bash
   # Nginx 日志
   tail -f /var/log/nginx/tutu_error.log
   
   # PHP 日志
   tail -f /www/wwwroot/tutu/server/runtime/log/*.log
   
   # MySQL 日志
   tail -f /var/log/mysql/error.log
   ```

2. **检查服务状态**
   ```bash
   systemctl status nginx
   systemctl status php-fpm
   systemctl status mysql
   systemctl status redis
   ```

3. **测试网络连通性**
   ```bash
   # 测试端口
   telnet 127.0.0.1 3306
   telnet 127.0.0.1 6379
   
   # 测试 API
   curl -I https://your-domain.com/api/index/index
   ```

4. **参考相关文档**
   - `DIAGNOSIS_REPORT.md` - 积分兑换功能问题诊断
   - `排行榜定时任务配置说明.md` - 排行榜配置
   - `消息通知定时任务配置指南.md` - 消息推送配置
   - `宝塔面板定时任务配置指南.md` - 宝塔配置指南

---

## 附录

### A. 目录结构说明

```
tutu/
├── server/                 # 后端服务
│   ├── app/               # 应用目录
│   │   ├── api/          # 用户端API
│   │   ├── platformapi/  # 平台管理API
│   │   ├── tenantapi/    # 租户管理API
│   │   └── common/       # 公共模块
│   ├── config/           # 配置文件
│   ├── public/           # 公共访问目录（Web根目录）
│   │   ├── platform/    # 平台管理端构建产物
│   │   ├── tenant/      # 租户端构建产物
│   │   └── uploads/     # 上传文件目录
│   ├── runtime/          # 运行时文件（日志、缓存）
│   ├── .env             # 环境配置（重要）
│   └── composer.json    # PHP依赖
│
├── platform/             # 平台管理端源码
│   ├── src/             # 源代码
│   ├── dist/            # 构建产物（自动生成）
│   ├── .env.production  # 生产环境配置
│   └── package.json     # 前端依赖
│
├── tenant/              # 租户管理端源码
│   ├── src/            # 源代码
│   ├── dist/           # 构建产物（自动生成）
│   ├── .env.production # 生产环境配置
│   └── package.json    # 前端依赖
│
└── user_client1/        # 小程序源码
    ├── pages/          # 页面
    ├── components/     # 组件
    ├── utils/          # 工具类
    ├── manifest.json   # 小程序配置
    └── package.json    # 依赖
```

### B. 端口说明

| 服务 | 默认端口 | 说明 |
|------|---------|------|
| Nginx | 80 | HTTP |
| Nginx | 443 | HTTPS |
| MySQL | 3306 | 数据库 |
| Redis | 6379 | 缓存 |
| PHP-FPM | 9000 | PHP处理 |

### C. 关键文件说明

| 文件 | 路径 | 说明 |
|------|------|------|
| 环境配置 | `server/.env` | 数据库、Redis等配置 |
| 入口文件 | `server/public/index.php` | PHP入口 |
| 路由配置 | `server/route/app.php` | 路由定义 |
| 前端配置 | `platform/.env.production` | 前端API地址 |
| 构建脚本 | `platform/scripts/release.mjs` | 自动发布脚本 |
| 小程序配置 | `user_client1/manifest.json` | 小程序AppID等 |

---

**文档版本**：v1.0.0  
**最后更新**：2025-12-27  
**维护者**：系统管理员

如有疑问，请查阅项目根目录下的其他技术文档。
